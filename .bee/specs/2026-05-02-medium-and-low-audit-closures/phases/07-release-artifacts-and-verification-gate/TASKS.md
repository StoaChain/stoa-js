# Phase 7: Release artifacts and verification gate -- Tasks

<!-- Combined Pass 1+2 output: 4 tasks grouped into 2 waves with dependency analysis and file-ownership conflict checks. Research notes derived directly from requirements.md REQ-14..REQ-17 plus the v2.2.0 Phase 2 release-artifacts precedent (`.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md` T2.10/T2.11/T2.12/T2.13). -->

## Goal

Produce ALL release artifacts (package.json version bump, CHANGELOG.md prepend, README.md updates) in the same atomic v2.3.0 commit so that the publish workflow's three-check version-parity gate at `.github/workflows/publish.yml:99/111/120` passes byte-identically, then run a comprehensive verification gate before the phase is considered complete. This is the LAST work phase of the spec — Phases 1 through 6 have already landed in the working tree and Phase 7 confirms the working tree is in commit-ready shape.

**Atomic-ship contract (NFR-06):** all 7 phases land in a SINGLE commit and a SINGLE annotated `v2.3.0` tag. Single `npm publish`. Single GitHub Release covers the entire spec. Phase 7 does NOT create the commit itself — the conductor (via `/bee:commit`) creates the commit after this phase's verification gate passes.

**Files in scope (RELEASE ARTIFACTS):**
- `Z:/OuronetCore/package.json` (version bump 2.2.0 → 2.3.0 — REQ-14)
- `Z:/OuronetCore/CHANGELOG.md` (prepend `## 2.3.0` entry — REQ-15)
- `Z:/OuronetCore/README.md` (Status block + version history paragraph + test-count refresh + NEW `./observability` submodule table row + optional "What's new in v2.3.0" section — REQ-16)

**Files in scope (VERIFICATION):**
- No file modifications by T7.4 — this task runs commands and pastes output as evidence (R8 discipline). Post-`npm test` placeholder patching modifies CHANGELOG.md and README.md to replace `≥{N}` / `+{M}` placeholders with actual integers.

**Cross-phase dependency block (CRITICAL — read before authoring T7.2 / T7.3 / T7.4):**

Phase 7 depends on ALL prior 6 phases having landed in the working tree before this phase runs. The atomic-ship contract (NFR-06) means all 7 phases share the same commit; the conductor's wave logic ensures Phases 1-6 have already produced their on-disk artifacts before Phase 7 spawns. T7.4's verification gate asserts the integrated state of all 7 phases:

- **Phase 1 (REQ-01):** `grep -r "function safeCreationTime" src/` returns exactly 1 hit (the canonical at `src/pact/format.ts:138-140`); the 11 inline copies in `src/interactions/*Functions.ts` are removed and replaced with imports from `../pact`.
- **Phase 2 (REQ-02 + REQ-03):** `src/codex/codec.ts` runtime shape validation is in place; `src/signing/codexStrategy.ts` foreign-key resolver pre-flight check is in place; `src/signing/types.ts` JSDoc is clarified. ≥3 new test cases pass.
- **Phase 3 (REQ-04..07):** `src/guard/smartAccountAuth.ts` `classifyGuardKind` is tightened; `normalizeKeysetRef` helper is added; `SmartAccountAuthPathsAnalysis` JSDoc enumerates 4 reachable states + optional `firstSignableButUnsatisfied` field; `src/guard/guardUtils.ts` throws new `UnknownPredicateError` and `analyzeGuard` folds it into `predicateRecognized: false`. `UnknownPredicateError` is re-exported from `src/guard/index.ts` (LOCKED per spec-review iter 1 I-002 — guard subpath, NOT errors subpath). ≥3 new test cases pass.
- **Phase 4 (REQ-08 + REQ-09):** `Z:/OuronetCore/README.md` header version table is refreshed; `Z:/OuronetCore/.bee/CONTEXT.md` interactions section describes v1.4 / v1.5 / v1.6 additions. Documentation only; zero code risk. NOTE: T7.3 below builds on top of Phase 4's README updates — T7.3 adds the v2.3.0 Status block and version-history paragraph; Phase 4's header version table refresh is DISTINCT and NOT re-touched by T7.3.
- **Phase 5 (REQ-10 + REQ-11):** `src/interactions/ouroFunctions.ts` 7 catch sites route via `getLogger().error(...)` from `../observability` (LOCKED iter 2 per spec-review I-001); `src/interactions/addLiquidityFunctions.ts` outer try/catch on `getLPTypeInfo` is dropped (Option A LOCKED). HARD source-level dependency on Phase 6 — both phases land in the same atomic commit.
- **Phase 6 (REQ-12 + REQ-13):** `src/reads/pactReader.ts` and `src/reads/rawCalibratedRead.ts` JSDoc lists canonical tier mapping (T1=balance, T2=preview, T3=metadata, T7=very-static); NEW FILES `src/observability/index.ts` (barrel) + `src/observability/logger.ts` (implementation) export `Logger` type, `setLogger` (with null-rejection per REQ-13 I-003), `getLogger` (default routes to `console.warn` / `console.error`); every `console.warn` and `console.error` in `src/` is rerouted through the seam (`grep -nE "console\.(warn|error)" src/` returns ZERO matches); `package.json` exports map gains a NEW `./observability` subpath pointing at `dist/observability/index.{js,d.ts}`. ≥3 new test cases pass.

**Locked decisions inherited from spec-review (CRITICAL — embed in CHANGELOG/README copy):**

- **REQ-13 null-rejection error message** is exactly `setLogger requires a non-null Logger` (LOCKED iter 1 per I-003). The CHANGELOG Added-section may quote this verbatim if useful but MUST NOT paraphrase it.
- **`UnknownPredicateError` is re-exported from `./guard`** (LOCKED iter 1 per I-002 — guard subpath, NOT errors subpath). The CHANGELOG Added-section text in `phases.md:68` commits to "the new typed predicate error class re-exported from the guard subpath" so a divergent landing location would invalidate the changelog.
- **Two-file source layout** for the observability seam: `src/observability/{index.ts,logger.ts}` produces `dist/observability/{index,logger}.{js,d.ts}` — all 4 files MUST be present after `npm run build`. T7.4's gate asserts this (LOCKED iter 1 per I-003).
- **F-CORE-019 catches route via `getLogger().error(...)`** (LOCKED iter 2 per I-004 — original audit-spec's "silent" alternative is REJECTED). The CHANGELOG Fixed section text MUST cite this routed convention.
- **F-CORE-021 outer try/catch is DROPPED (Option A)** — NOT commented as belt-and-braces (LOCKED per I-001). The CHANGELOG Fixed section text MUST cite Option A.
- **NODE_ENV runtime warning on tier-less reads is OUT OF SCOPE for REQ-12** (LOCKED iter 1 per advisory #2). JSDoc-only clarification. The CHANGELOG Fixed section MUST NOT mention any runtime warning.
- **Test-count placeholder convention (inherited from v2.2.0 T2.11/T2.12/T2.13):** T7.2 and T7.3 land literal placeholder tokens `≥{N}` and `+{M}` (NOT hardcoded numbers); T7.4 patches them with actual integers from `npm test` output. Bold wraps the NUMBER ONLY, not the word "passing" — matches the v2.2.0 convention at `CHANGELOG.md:19` style. **Baseline shift:** v2.3.0's `M = N - 458` (delta from v2.2.0's 458-test baseline visible at `README.md:128` `**458 tests** pass on every commit`).

## Wave 1 (parallel — 3 file-disjoint tasks)

- [x] T7.1 | Bump `package.json` `version` field from `2.2.0` to `2.3.0` (REQ-14) | bee-implementer
  - requirements: [REQ-14]
  - acceptance:
    - `Z:/OuronetCore/package.json` line 3 (the `"version"` field) is changed from `"2.2.0"` to `"2.3.0"`. NO other field in `package.json` is modified by this task — in particular, the new `./observability` subpath added to the `exports` map is OWNED BY PHASE 6 (REQ-13), NOT by this task. If `./observability` is missing from the exports map at the time T7.1 runs, that is a Phase 6 deficiency to surface up — DO NOT add it here.
    - The change is observable by the verification gate via `node -e "console.log(require('./package.json').version)"` returning `2.3.0`.
    - Implementer pastes the literal output `2.3.0` as evidence (R8).
    - Implementer runs a paranoid `git diff package.json` and pastes the output — the ONLY change shown should be the version-field single-line edit. If any other line shows up in the diff (whitespace, exports-map churn, peer-dep bump), the implementer reports the unexpected change and STOPS rather than landing it. The version-parity check at `.github/workflows/publish.yml` reads `package.json` via `node -p "require('./package.json').version"` and compares to the `v*` tag — drift would fail the publish step.
    - This task is INDEPENDENT of T7.2 (CHANGELOG) and T7.3 (README) at the file level — they touch different files. They share a logical contract (version parity) which the verification gate (T7.4) asserts after all three land.
    - Implementer does NOT run `npm test`, `npm run typecheck`, or `npm run build` — those run in T7.4 (the conductor's wave-2 verification gate).
  - context: requirements.md REQ-14; CLAUDE.md "Publishing flow" section (`The version-parity check is load-bearing — never push a tag whose number disagrees with package.json`); current `Z:/OuronetCore/package.json:3` reads `"version": "2.2.0"` (verified at start of Phase 7); v2.2.0 precedent `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md` T2.10 (the same task pattern at the prior version boundary).
  - research:
    - Pattern: [CITED] No prior pattern needed — pure version-string edit. `Z:/OuronetCore/package.json:3` currently reads `"version": "2.2.0"` (verified). Single-character edit on the patch digit position: `"2.2.0"` → `"2.3.0"`.
    - Reuse: [CITED] Verification command from CLAUDE.md "Publishing flow": `node -p "require('./package.json').version"` returns the version string. The publish workflow's parity check reads the same file via the same approach.
    - Reuse: [CITED] v2.2.0 T2.10 acceptance from `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md:246-261` — the prior bump (`2.1.2` → `2.2.0`) used the same single-line edit pattern. Mirror it exactly: change ONLY the version string, paste `git diff` as evidence to prove no other field was touched.
    - Types: N/A — JSON literal edit.
    - Approach: [LOCKED] Exact version `2.3.0` per REQ-14 — MINOR additive bump from `2.2.0`. Strict semver per CLAUDE.md "Versioning discipline" (additive-only release closing 13 audit findings; no breaking changes; new public surfaces are additive). DO NOT touch any other field. The new `./observability` subpath in the exports map is Phase 6's responsibility (REQ-13) — verify (read-only) that it is already present at lines ~57-60 by the time this task runs; if missing, surface as a Phase 6 deficiency and STOP.

  - notes:

- [x] T7.2 | Prepend new `## 2.3.0 — YYYY-MM-DD` entry to `CHANGELOG.md` per locked v2.2.0 single-concern minor format (Lead / Added / Fixed grouped by milestone / Stats) | bee-implementer
  - requirements: [REQ-15]
  - acceptance:
    - `Z:/OuronetCore/CHANGELOG.md` is modified by prepending a new top entry BEFORE the existing `## 2.2.0` entry at line 5. The new entry uses the date format `## 2.3.0 — YYYY-MM-DD` where `YYYY-MM-DD` is the current date `2026-05-02` (the implementer reads the current date from the system). If the actual ship date slips to a later day, use the actual ship date.
    - The new entry has the following structure (mirrors v2.2.0 single-concern minor format at `CHANGELOG.md:5-29` — verified pattern):
      - **Lead paragraph (1-3 sentences)** describing the additive nature of the release. Required content: closes 13 audit findings (7 MEDIUM tier + 6 LOW tier from the 2026-04-30 audit cycle); introduces 2 NEW PUBLIC SURFACES — (1) typed `UnknownPredicateError` class re-exported from the `./guard` subpath, (2) NEW `./observability` subpath with `Logger` type + `setLogger` mutator + `getLogger` accessor mirroring the existing `setPactReader` injection-seam pattern. Note that all changes are additive (no public exports removed; no return types widened; existing `instanceof Error` checks and existing analysis-flag access continue to work).
      - **`### Added (public surface)`** section listing both new surfaces with one-line descriptions:
        - `UnknownPredicateError` — `@stoachain/ouronet-core/guard`. Thrown by `computeThreshold` when it encounters an unrecognized predicate. The general-purpose `analyzeGuard` catches it and folds it into a `predicateRecognized: false` bit on the returned analysis (replaces the previous silent `console.warn` diagnostic).
        - NEW `./observability` subpath — `@stoachain/ouronet-core/observability`. Exports `Logger` type (`{ warn(msg, ...args), error(msg, ...args) }`), `setLogger(logger: Logger): void` (throws `TypeError` with message exactly `setLogger requires a non-null Logger` on null/undefined input — LOCKED per REQ-13 I-003), `getLogger(): Logger` (returns the currently-configured logger; default routes `warn` to `console.warn` and `error` to `console.error`). Mirrors the existing `setPactReader` seam at `src/reads/pactReader.ts:33-71`.
        - Optional `firstSignableButUnsatisfied: number` field on `SmartAccountAuthPathsAnalysis` (`@stoachain/ouronet-core/guard`) — names the index of the first signable-but-unsatisfied path; `undefined` if none. Additive, opt-in.
      - **`### Fixed`** section grouping the closed findings by milestone:
        - **M1 (MEDIUM tier — 7 findings):**
          - **F-CORE-013 — Codex shape validation.** `deserializeCodex` at `src/codex/codec.ts:75-93` gains runtime shape checks after the version check. Validates that `kadenaWallets`, `ouronetWallets`, `addressBook` are arrays and `uiSettings` is an object. Domain-prefixed errors NAME the offending field but never echo its value (preserves the secrets-stay-out-of-telemetry boundary). Forward-compat preserved: extra unknown top-level fields survive the deserialize round-trip exactly as before (v1.2 envelope contract unchanged).
          - **F-CORE-014 — Foreign-key resolver pre-flight.** `CodexSigningStrategy` at `src/signing/codexStrategy.ts:180-182` gains a pre-flight check: when a transaction requires a foreign-key signer AND `this.resolver.requestForeignKey` is undefined, throws a precise error before reaching `universalSignTransaction`. JSDoc on `KeyResolver.requestForeignKey` at `src/signing/types.ts:62-69` is clarified to state the optional-at-the-interface-but-required-at-execute-time-when-needed contract. Server resolvers that omit `requestForeignKey` AND receive a foreign-key transaction now get a clear error instead of an opaque deep-stack failure.
          - **F-CORE-015 — `safeCreationTime` DRY refactor.** Removed 11 byte-identical inline `function safeCreationTime` declarations across `src/interactions/*Functions.ts` (activate, addLiquidity, coil, crossChain, dex, guard, kpay, ouro, pension, urStoa, wrap). All 11 files now import the canonical declaration from `src/pact/format.ts:138-140` (single source of truth). Mechanical refactor with byte-identical behavior; zero test count change for this finding.
          - **F-CORE-016a — Tightened `classifyGuardKind`.** Requires the FULL minimal shape per kind: capability needs `cgName` + `cgArgs` + `cgPactId`; user needs `fun` + `args`; keyset needs `pred` + `keys`; keyset-ref accepts either casing of the ref field. Under-specified guard shapes that previously silently mis-classified now classify as `unknown` and are surfaced to the caller.
          - **F-CORE-016b — Keyset-reference casing normalization.** New `normalizeKeysetRef` helper applied at the `resolveGuard` boundary (`src/guard/smartAccountAuth.ts:118-126`) so internal code only sees the camelCase form. Maps the lowercase chain-native `keysetref` → camelCase `keysetRef` at the chain-IO boundary.
          - **F-CORE-016c — `SmartAccountAuthPathsAnalysis` 4 reachable states.** JSDoc at `src/guard/smartAccountAuth.ts:209-240` enumerates the 4 reachable states: `firstSatisfied >= 0`; `firstSatisfied === -1 && anyKeyBased === true`; `firstSatisfied === -1 && anyKeyBased === false && anyKnownKind`; all-unknown. Optional `firstSignableButUnsatisfied: number` field added to the analysis surface.
          - **F-CORE-017 — `UnknownPredicateError` typed class + `predicateRecognized` flag.** `computeThreshold` at `src/guard/guardUtils.ts:76-79` throws the new typed `UnknownPredicateError` (additive public class re-exported from `./guard`). `analyzeGuard` catches the class and folds it into a structured `predicateRecognized: false` bit on the returned analysis. The previous silent `console.warn` is removed.
        - **M2 (LOW tier — 6 findings):**
          - **F-CORE-018a — README header version table refresh.** `Z:/OuronetCore/README.md` header version table updated from its v1.3.0 / v1.4.0 baseline to current v2.2.0 reality, cross-referencing `CHANGELOG.md` for per-version detail. (Phase 4 deliverable.)
          - **F-CORE-018b — CONTEXT.md interactions section refresh.** `Z:/OuronetCore/.bee/CONTEXT.md` interactions section updated to describe v1.4 (`AccountSelectorData` `public-key` / `sovereign` / `governor` fields), v1.5 (Leto / Artemis / Apollo re-exports + `createGen1Primitive` factory + `AddressPrefixPair` type), v1.6 (Smart Ouronet Account auth-path resolution primitives + `buildRotateSovereignPactCode`).
          - **F-CORE-019 — Catch-block consistency in `ouroFunctions.ts`.** All 7 affected catch sites in `src/interactions/ouroFunctions.ts` now route via `getLogger().error(...)` from `../observability` (LOCKED iter 2 per spec-review I-001 — both phases land in the same atomic commit; the routed convention is the higher-value outcome). Convention is documented in a code comment near the affected handlers.
          - **F-CORE-020 — Tier semantics JSDoc.** `src/reads/pactReader.ts` and `src/reads/rawCalibratedRead.ts:40-46` JSDoc enumerates the canonical tier mapping (T1=balance, T2=preview, T3=metadata, T7=very-static, matching OuronetUI's reader). Documents that the default reader accepts and ignores the `tier` argument and cross-references `setPactReader` for cache-aware consumers.
          - **F-CORE-021 — Drop dead try/catch in `getLPTypeInfo`.** Outer try/catch wrapping `getLPTypeInfo`'s `Promise.all` (structural anchor — actual line range 219-257 in current file; cross-plan auto-fix iter 1 per CI-001) was dead code (two never-rejecting promises cannot themselves reject) and is removed (Option A LOCKED per spec-review iter 1 I-001 — the "comment as belt-and-braces" alternative was explicitly rejected). Future regressions in inner catches surface as real test failures rather than silent masking.
          - **F-CORE-022 — Central logger seam at `./observability`.** New two-file source layout `src/observability/{index.ts,logger.ts}` exposes `Logger` / `setLogger` / `getLogger` mirroring the `setPactReader` pattern. Default routes to `console.warn` / `console.error`. `package.json` exports map gains the new `./observability` subpath. Every `console.warn` and `console.error` in `src/` (~25 hits across ~8 files per the audit) is rerouted through the seam — verifiable post-phase by `grep -nE "console\.(warn|error)" src/` returning ZERO matches.
      - **`### Stats`** block:
        - Files changed: list (NEW: `src/observability/index.ts`, `src/observability/logger.ts`, plus any new test files added during Phases 2/3/6 — implementer reads `git status --short` post-phase to enumerate; MODIFIED: `src/codex/codec.ts`, `src/signing/codexStrategy.ts`, `src/signing/types.ts`, 11× `src/interactions/*Functions.ts`, `src/guard/smartAccountAuth.ts`, `src/guard/guardUtils.ts`, `src/guard/index.ts`, `src/reads/pactReader.ts`, `src/reads/rawCalibratedRead.ts`, `src/network/nodeFailover.ts` (and any other files touched by the console-sweep), `src/interactions/ouroFunctions.ts`, `src/interactions/addLiquidityFunctions.ts`, `Z:/OuronetCore/README.md`, `Z:/OuronetCore/.bee/CONTEXT.md`, `package.json`, `CHANGELOG.md`).
        - Test count: **≥{N}** passing (up from `458` v2.2.0 baseline; +{M} new in v2.3.0). T7.2 lands the `**≥{N}**` and `+{M}` literal placeholder tokens; T7.4 patches them with `**N**` and `+M` post-test-run, where `M = N - 458` (delta from v2.2.0 baseline visible at `README.md:128` `**458 tests** pass on every commit`). **Bold wraps NUMBER ONLY, not the word "passing"** — matches the v2.2.0 convention at `CHANGELOG.md:19`. DO NOT hardcode a specific number; the placeholder MUST be a literal `**≥{N}**` and `+{M}` token for T7.4's patching grep to find and replace.
        - No public-API removals. The 2 new public surfaces (`UnknownPredicateError` class on `./guard`; `Logger` type + `setLogger` + `getLogger` on the new `./observability` subpath) are additive; no existing exports change shape; no return types widened; no breaking changes for downstream consumers (`OuronetUI`, `AncientHolder HUB`).
    - The existing `## 2.2.0` entry at line 5 (and all prior entries) are PRESERVED unchanged below the new entry. NO edits to historical entries.
    - The first heading-line of the new entry is exactly `## 2.3.0 — 2026-05-02` (or the actual ship date if it slips). The publish workflow's version-parity grep at `.github/workflows/publish.yml:120` (`grep -m1 '^## ' CHANGELOG.md | grep -qE "^## ${TAG_VERSION}[[:space:]]"`) MUST find this line — verified by T7.4.
    - Implementer runs `head -50 CHANGELOG.md` and pastes the first 50 lines as evidence the entry landed correctly (R8).
    - Implementer does NOT run `npm test`, `npm run typecheck`, or `npm run build` — those run in T7.4. Implementer also does NOT replace the `≥{N}` / `+{M}` placeholders — that is T7.4's responsibility post-`npm test`.
  - context: requirements.md REQ-15; existing CHANGELOG `Z:/OuronetCore/CHANGELOG.md:1-29` (verified the v2.2.0 / v2.1.2 / v2.1.0 format conventions — Lead paragraph + `### Added (public surface)` + `### Fixed` + `### Stats` is the established pattern); v2.2.0 T2.11 acceptance from `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md:263-297` (the placeholder token convention `**≥{N}** passing` and `+{M} new` patched by T2.13); the publish workflow's three-check version-parity gate at `.github/workflows/publish.yml:99/111/120` (the `## ${TAG_VERSION}[[:space:]]` heading anchor at line 120 specifically); spec.md F-CORE-018a + F-CORE-019 + F-CORE-021 LOCKED rationale to embed in the `### Fixed` section.
  - research:
    - Pattern: [CITED] `Z:/OuronetCore/CHANGELOG.md:5-29` — v2.2.0 entry establishes the current minor-release format: `## VERSION — YYYY-MM-DD` heading, bold lead paragraph (1-3 sentences with closure citation), `### Added (public surface)` section with bold-symbol-name bullets, `### Fixed` section with bold-finding-id bullets (this release groups by milestone — M1 / M2 — for the 13-finding sweep), `### Stats` section with file count + lines + test count placeholder. Mirror this structure exactly.
    - Pattern: [CITED] v2.2.0 T2.11 acceptance lines 263-297 — establishes the placeholder-token convention: `**≥{N}** passing` and `+{M} new in vX.Y.Z` are LITERAL strings T7.2 lands; T7.4 (this phase's verification gate) patches them with actual integers post-`npm test`. The `≥` Unicode glyph is part of the placeholder pattern; T7.4's grep at the patching step looks for `≥\{N\}|\+\{M\}|\{N\}|\{M\}`.
    - Pattern: [CITED] `Z:/OuronetCore/CHANGELOG.md:5` — `## 2.2.0 — 2026-05-02` is the workflow gate's exact heading match for v2.2.0. The new heading `## 2.3.0 — 2026-05-02` is the v2.3.0 equivalent; the workflow grep `^## ${TAG_VERSION}[[:space:]]` requires whitespace (em-dash `—` or space) after the version digits, which the `— 2026-05-02` form satisfies.
    - Reuse: [CITED] Date format `2026-05-02` per current-date in `<env>` block (system-supplied) and per the spec folder name `2026-05-02-medium-and-low-audit-closures`. The new heading `## 2.3.0 — 2026-05-02` is the workflow gate's exact match (per `.github/workflows/publish.yml:118-126` — first `## ` heading must equal `## ${TAG_VERSION}[[:space:]]`).
    - Reuse: [CITED] M1 / M2 milestone grouping is NEW — the v2.2.0 entry had a flat `### Fixed` list because it closed only 3 findings (F-CORE-009 / 011 / 012). This release closes 13 findings split across 7 (M1 MEDIUM) + 6 (M2 LOW), so the milestone grouping is justified as a structural extension; subheadings under `### Fixed` use **bold** `**M1 (MEDIUM tier — 7 findings):**` / `**M2 (LOW tier — 6 findings):**` to maintain the bold-finding-id bullet pattern visually consistent.
    - Reuse: [CITED] LOCKED-decision text to embed verbatim: F-CORE-019 catches "route via `getLogger().error(...)` from `../observability`" (LOCKED iter 2 per I-001); F-CORE-021 outer try/catch is "removed (Option A LOCKED per spec-review iter 1 I-001 — the "comment as belt-and-braces" alternative was explicitly rejected)"; REQ-13 null-rejection error message is exactly `setLogger requires a non-null Logger` (LOCKED iter 1 per I-003).
    - Types: N/A — Markdown content only.
    - Approach: [LOCKED] 7 closures cited in M1 verbatim from requirements.md REQ-02..REQ-07 (F-CORE-013, F-CORE-014, F-CORE-015, F-CORE-016a, F-CORE-016b, F-CORE-016c, F-CORE-017); 6 closures cited in M2 verbatim from requirements.md REQ-08..REQ-13 (F-CORE-018a, F-CORE-018b, F-CORE-019, F-CORE-020, F-CORE-021, F-CORE-022). 2 new public surfaces in Added section: (1) `UnknownPredicateError` from `./guard` (Phase 3 deliverable); (2) `Logger` / `setLogger` / `getLogger` on new `./observability` subpath (Phase 6 deliverable). `firstSignableButUnsatisfied` field is mentioned in Added as it is also additive. Test count placeholder `**≥{N}** passing` lands literally — T7.4 patches it. Stats block file-list cites NEW + MODIFIED files at the level of detail the v2.2.0 entry uses.

  - notes:

- [x] T7.3 | Update `README.md` per locked maintenance rule: Status block leads with 2.3.0 + version history paragraph + test-count refresh + NEW `./observability` submodule table row + optional "What's new in v2.3.0" section | bee-implementer
  - requirements: [REQ-16]
  - acceptance:
    - `Z:/OuronetCore/README.md` is modified per the v2.1.0+ locked maintenance rule (memory: "README must be updated on every version bump"). Five updates land — four required, one optional-but-shipped (per v2.1.0 / v2.2.0 precedent):
      1. **`## Status` block (lines ~7-19 of current README) leads with `2.3.0`:** the opening line `**\`2.2.0\` on public npmjs**` is changed to `**\`2.3.0\` on public npmjs**`. The summary paragraph is rewritten to lead with the v2.3.0 additive nature: closes 13 audit findings (7 MEDIUM tier + 6 LOW tier from the 2026-04-30 audit cycle); introduces 2 new public surfaces — typed `UnknownPredicateError` class on `./guard` and NEW `./observability` subpath with `Logger` / `setLogger` / `getLogger` mirroring the `setPactReader` injection-seam pattern. Plus a sentence pointing to `CHANGELOG.md` for the full v2.3.0 entry, AND a sentence noting that `v2.2.x` consumers upgrade transparently — `instanceof Error`, existing analysis-flag access, and existing `error.message` access still work; the new typed-class discrimination and logger-seam injection are opt-in.
      2. **Version history extended with v2.3.0 paragraph:** the version-history paragraphs (currently ending with `**v2.2.0**` at lines ~99-134 of the current README) gain a new paragraph after v2.2.0 describing v2.3.0. Format mirrors v2.1.0 / v2.2.0: bold version + paragraph(s) describing the milestone grouping (M1 — 7 MEDIUM; M2 — 6 LOW), the 2 new public surfaces, the central logger seam at `./observability`, the safeCreationTime DRY refactor, the codex shape validation, the foreign-key resolver pre-flight, the guard hardening (4-state JSDoc + `firstSignableButUnsatisfied`), the `UnknownPredicateError` typed class, and the catch-block consistency cleanup. The phrasing should be standalone-readable (someone landing on the npm page who does not read the changelog still gets the gist). The paragraph MUST start with `**v2.3.0**` followed by at least one whitespace character to satisfy the workflow gate at `.github/workflows/publish.yml:111` (`grep -qE "^\*\*v2\.3\.0\*\*[[:space:]]" README.md`).
      3. **Test-count references refreshed (placeholder convention from v2.2.0 T2.12 — F-002 / F-PAT-002 verbatim-match patterns; baseline shifted to v2.2.0 per the v2.3.0-only delta scope):** all `458` and `+72 new` references at lines ~128-130 and ~313 are updated to the new total post-T7.4 verification. T7.3 lands a CLEAR PLACEHOLDER (`≥{N}` form) that T7.4 patches with the exact count post-`npm test`. **Two known sites:**
         - Line ~128: `**458 tests** pass on every commit (up from 386 v2.1.2 baseline; +72 new tests across \`tests/{pact-reader,wallet-builder,transaction-errors,seed-type-migration}.test.ts\` and extensions to \`tests/{encryption,encryption-upgrade,codex-codec,cfm-builders,pact-format}.test.ts\`).` becomes `**≥{N} tests** pass on every commit (up from 458 v2.2.0 baseline; +{M} new tests across the v2.3.0 audit-closure surfaces — codex-codec shape validation, foreign-key resolver pre-flight, guard classification + keyset-ref normalization + 4-state analysis + UnknownPredicateError, observability seam contract).` where T7.3 leaves `{N}` and `{M}` as literal placeholder strings (T7.4 patches with actual count from `npm test`). **NO build-internal text leaks into the published README** (e.g., do NOT inline raw it-block floor counts or per-task spec instructions — F-PAT-005 / F-303 lock from v2.2.0 prevents that). The `+M` derived counter math: **`M = N - 458` (delta from v2.2.0 baseline; matches the v2.3.0-only file-list scope)**. Diverges from the cumulative-baseline form because v2.3.0's NEW tests come from Phase 2 (≥3) + Phase 3 (≥3) + Phase 6 (≥3) per NFR-04, ~9 floor + extensions on existing tests if any. The actual count is reported by T7.4.
         - Line ~313 (the README "Quick start" / build/test recipe block, currently `vitest run — 458 tests across crypto, guard, gas, ...`): becomes `vitest run — ≥{N} tests across crypto, guard, gas, pact format, signing, strategy, codex, cfmBuilders, dalos integration, wallet, interactions-read-seam, network, failover-client, timeouts, failover-submit, pact-reader, wallet-builder, transaction-errors, seed-type-migration, observability` where T7.3 appends `, observability` (or whatever Phase 6's test file is named — verify by reading Phase 6's TASKS.md research notes for the test file basename) as a permanent edit (not a patched placeholder), and leaves `≥{N}` as the patched placeholder. Other v2.3.0 phases may add additional test files (e.g., codex-shape, guard-tightening, smartAccountAuth-states) — implementer reads `git status --short tests/` post-phase to enumerate any new test file basenames and append them to the comma-separated list.
         - **DO NOT hardcode a specific count** — use the `≥{N}` placeholder verbatim. T7.4 computes `M = actual_count - 458` post-test-run and patches both placeholder sites with the exact integers (and `≥{N}` becomes `N`).
      4. **NEW submodule table row for `./observability`:** the submodule table (currently ending at line ~324 with the `./dalos` row) gains a new row for `./observability`. Insert the new row in the natural alphabetical / functional position — preferably immediately after the `./reads` row (since both are runtime-injection seams) or at the end of the table. Format mirrors the existing rows: `| @stoachain/ouronet-core/observability | **(v2.3.0+)** central logger seam mirroring `setPactReader`. Exports `Logger` type (`{ warn(msg, ...args), error(msg, ...args) }`), `setLogger(logger)` mutator (throws `TypeError("setLogger requires a non-null Logger")` on null/undefined), `getLogger()` accessor. Default routes `warn` to `console.warn` and `error` to `console.error` — consumers who do nothing observe identical behavior to direct `console.*` calls. |`. The implementer escapes Markdown table-pipes inside the code-fenced text by using backticks (the existing rows demonstrate this pattern at lines 314-324).
      5. **Optional "What's new in v2.3.0" section:** a new H2 section between the existing v2.2.0 "What's new" (currently at lines ~208-241) and the "Migrating to v2.x" section (line ~243). Format mirrors the v2.2.0 "What's new" — short prose lead + a copy-paste TypeScript example showing how a consumer routes log events via `setLogger(...)`:
        ```ts
        import { setLogger, getLogger, type Logger } from "@stoachain/ouronet-core/observability";

        // Option 1: Forward to your existing app logger (Pino, Winston, Sentry, …).
        const myLogger: Logger = {
          warn: (msg, ...args) => myApp.warn(msg, args),
          error: (msg, ...args) => myApp.error(msg, args),
        };
        setLogger(myLogger);

        // Option 2: Capture for telemetry without losing console output.
        const original = getLogger();
        setLogger({
          warn: (msg, ...args) => { telemetry.warn(msg, args); original.warn(msg, ...args); },
          error: (msg, ...args) => { telemetry.error(msg, args); original.error(msg, ...args); },
        });

        // Or do nothing — the default routes to console.warn / console.error so
        // existing v2.2.x behavior is preserved exactly.
        ```
        Plus a 1-sentence note that `instanceof Error` checks and existing analysis-flag access continue to work for un-upgraded consumers; the typed `UnknownPredicateError` discrimination from `./guard` and the logger-seam injection from `./observability` are both purely opt-in. The optional section is nice-to-have per the spec wording — implementer ships it because v2.1.0 / v2.2.0 precedent established the pattern and consumers find these blocks useful.
    - The publish workflow's three-check version-parity grep gate is satisfied (verbatim-match the workflow patterns at `.github/workflows/publish.yml:99/111/120`):
      - Check 1 (Status block): `grep -qE "\`2\.3\.0\`[[:space:]]+on public npmjs" README.md` returns the Status-block line (✓ from update 1). The Status block MUST have whitespace (one-or-more) between `` `2.3.0` `` and `on public npmjs` per the workflow's `[[:space:]]+` anchor.
      - Check 2 (version-history paragraph): `grep -qE "^\*\*v2\.3\.0\*\*[[:space:]]" README.md` returns the version-history paragraph lead (✓ from update 2). The paragraph MUST be `**v2.3.0**` followed by at least one whitespace character.
      - Check 3 (CHANGELOG first heading): T7.2's responsibility — `grep -m1 '^## ' CHANGELOG.md | grep -qE "^## 2\.3\.0[[:space:]]"`. T7.3 does NOT touch CHANGELOG.md.
    - Implementer runs the two README grep checks (checks 1 + 2) AND pastes their output as evidence (R8). T7.4 re-runs all three as a redundant safety net.
    - Implementer does NOT change any other section of the README — the `## Status` paragraph rewrite, the version-history paragraph addition, the test-count placeholders, the `./observability` submodule table row, and the optional "What's new in v2.3.0" section are the only edits. The migration guide, design-docs links, npm-link recipe, publishing-flow sections, prior-version "What's new" blocks, and the existing submodule table rows all stay untouched.
    - Implementer does NOT run `npm test`, `npm run typecheck`, or `npm run build` — those run in T7.4. Implementer also does NOT replace the `≥{N}` / `+{M}` placeholders — that is T7.4's responsibility post-`npm test`.
  - context: requirements.md REQ-16; spec.md "Release artifacts" third bullet (locked maintenance rule details); existing README `Z:/OuronetCore/README.md:1-359` (verified Status block at lines 7-19, version-history at lines 25-134, test-count refresh sites at lines ~128 and ~313, submodule table at lines 312-324, "What's new in v2.2.0" section at lines 208-241); CLAUDE.md memory note ("README must be updated on every version bump"); the publish workflow's three-check gate at `.github/workflows/publish.yml:99/111/120`; v2.2.0 T2.12 acceptance from `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md:299-343` (the established placeholder pattern + `What's new` section structure).
  - research:
    - Pattern: [CITED] `Z:/OuronetCore/README.md:7-19` — `## Status` block format: opening line `**\`VERSION\` on public npmjs**` followed by paragraph describing the release. The version-history paragraphs at lines 25-134 mirror this structure for prior releases (e.g., v2.1.0 at lines 74-86, v2.1.2 at lines 88-97, v2.2.0 at lines 99-134). Test-count refresh sites at lines ~128 (`**458 tests** pass on every commit (up from 386 v2.1.2 baseline; +72 new`) and line ~313 (`vitest run — 458 tests across...`).
    - Pattern: [CITED] `Z:/OuronetCore/README.md:208-241` — existing `## What's new in v2.2.0` section establishes the optional pattern (short prose lead + copy-paste TypeScript example with imports from the new public surface). The new `## What's new in v2.3.0` section follows the same shape.
    - Reuse: [CITED] Submodule table at `Z:/OuronetCore/README.md:312-324` — current rows demonstrate the `| @stoachain/ouronet-core/SUBPATH | DESCRIPTION |` format. The `./reads` row at line ~321 is a structural sibling (also a runtime-injection seam) — preferred insertion position is immediately after that row OR at end of table. NEW row format follows the existing **(vX.Y.Z+)** annotation convention (e.g., the `./crypto` row's `**(v2.2.0+)** typed error classes ...`).
    - Reuse: [CITED] Workflow-gate three checks at `.github/workflows/publish.yml:91-128`: (1) `grep -qE "\`${TAG_VERSION}\`[[:space:]]+on public npmjs" README.md` — Status block; (2) `grep -qE "^\*\*v${TAG_VERSION}\*\*[[:space:]]" README.md` — version-history paragraph lead; (3) first `## ` heading in CHANGELOG.md matches `## ${TAG_VERSION}[[:space:]]`. The README must satisfy gates 1+2 — gate 3 is T7.2's responsibility.
    - Reuse: [CITED] v2.2.0 T2.12 placeholder convention from `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md:299-343` — `**≥{N} tests**` and `+{M} new tests across` are LITERAL placeholder strings T7.3 lands; T7.4 patches them post-`npm test`. The `≥` Unicode glyph is part of the placeholder pattern. Baseline-shift form per F-302: T7.3 cites `up from 458 v2.2.0 baseline` (NOT cumulative-since-v1.x), `M = N - 458`.
    - Types: N/A — Markdown content only.
    - Approach: [LOCKED] Per `feedback_readme_versioning.md` (memory): README is part of the release deliverable, not optional. All 4 required updates land in lockstep with package.json + CHANGELOG. Optional 5th update (`What's new in v2.3.0`) ships per acceptance because v2.1.0 / v2.2.0 precedent established consumer expectation. Test count `≥{N}` placeholder — T7.4 pulls exact count from gate verification post-`npm test` and patches.

  - notes:

## Wave 2 (depends on all of Wave 1)

- [x] T7.4 | Verification gate — run typecheck + full test suite + clean build + 4-file dist assertion + runtime export verification + 3 workflow-gate version-parity grep checks + Phase 1 + Phase 6 sweep grep checks + post-test placeholder patching | bee-implementer | needs: T7.1, T7.2, T7.3
  - requirements: [REQ-17]
  - acceptance:
    - **Quality-gate commands (paste output as evidence — R8):**
      - `npm run typecheck` exits 0. Implementer pastes the actual command output. ANY non-zero exit means a regression has been introduced by Phases 1-6 — implementer reports the failure with full output rather than claiming success.
      - `npm test` exits 0 on Linux/macOS. On Windows non-en-US, the documented locale-sensitive `tests/gas.test.ts > formatMaxFee` failure is the SOLE allowed non-zero exit (locked from v2.1.2 / v2.2.0 per ROADMAP.md success criterion 5) — implementer pastes the full vitest summary showing exactly that one failure (or a clean exit 0 if running on en-US Windows or non-Windows). The total passing count must be **≥467** (LOCKED iter 1 per cross-plan CI-004 — strict NFR-04 floor: v2.2.0 baseline 458 + ≥3 from Phase 2 + ≥3 from Phase 3 + ≥3 from Phase 6 = ≥467 minimum). Per-phase commitments overshoot this floor: Phase 2 commits to +8, Phase 3 to +10, Phase 6 to +4 → realistic floor ≥480. The earlier conservative ≥461 was arithmetically inconsistent with NFR-04 and was rejected. Implementer reports the exact count.
      - **Clean-before-build (REQUIRED):** `npm run clean` exits 0 (deletes `dist/`). Implementer pastes the command output (or notes "clean" with no output if rimraf produces none). This step is REQUIRED to prevent stale `dist/` artifacts from masking missing files in the next build (e.g., a missing `dist/observability/index.d.ts` could be undetected if a prior build's file is still on disk).
      - `npm run build` exits 0. **Explicit 4-file dist assertion (LOCKED iter 1 per I-003 — the new `./observability` subpath produces 4 compiled artifacts):** all 4 files MUST be present after the clean rebuild. Implementer runs:
        ```bash
        for f in dist/observability/index.js dist/observability/index.d.ts dist/observability/logger.js dist/observability/logger.d.ts; do
          test -f "$f" && echo "$f present" || echo "$f MISSING — gate FAIL"
        done
        ```
        and pastes the output. Expected: all 4 lines say "present". If any file is missing, the gate FAILS — Phase 6's two-file source layout (`src/observability/{index.ts,logger.ts}`) is broken or `tsconfig.build.json` is misconfigured. Implementer reports the failure with full output.
      - **Runtime export shape verification (use dynamic ESM import since the package is native ESM per `package.json:5` `"type": "module"`):**
        ```bash
        node -e "import('./dist/observability/index.js').then(m => console.log(typeof m.setLogger === 'function' && typeof m.getLogger === 'function'))"
        ```
        Implementer pastes literal `true` as the printed output. (Auto-fix iter 1 per D-001 — removed duplicate `setLogger` clause that was a copy-paste typo; `Logger` is a TypeScript type-only export and is verified by the `.d.ts` grep below, not by runtime `typeof` since types don't exist at runtime.) Then the typed-symbol check:
        ```bash
        grep -E "Logger|setLogger|getLogger" dist/observability/index.d.ts
        ```
        Implementer pastes the matched lines confirming `Logger` type, `setLogger` function, `getLogger` function are all exported from the barrel.
      - **Test wall-time check (NFR-03 — under 30s on Node 22 / Ubuntu):** implementer pastes the vitest "Duration" line from the summary. If the value is <30s on a non-Windows runner, the criterion is met; if >25s, implementer notes the trend in the task `notes:` field for future awareness. The 30s ceiling is the hard fail; the 25s warning is informational.
    - **Workflow-gate version-parity checks (VERBATIM-MATCH the workflow patterns at `.github/workflows/publish.yml:99/111/120`):**
      - Check 1 (Status block): `grep -qE "\`2\.3\.0\`[[:space:]]+on public npmjs" README.md && echo "check1 PASS"` returns "check1 PASS". Implementer pastes the matching README line via `grep -nE "\`2\.3\.0\`[[:space:]]+on public npmjs" README.md`. The workflow tolerates spaces, tabs, or multiple whitespace; the local check MUST use the same `[[:space:]]+` anchor so a multi-space README does not pass T7.4 but fail the workflow.
      - Check 2 (version-history paragraph): `grep -qE "^\*\*v2\.3\.0\*\*[[:space:]]" README.md && echo "check2 PASS"` returns "check2 PASS". Implementer pastes the matching line. Note the trailing `[[:space:]]` (single whitespace) after `**v2.3.0**` — a README with `**v2.3.0**\n` (immediate newline, no space) would pass a relaxed pattern but fail the workflow gate.
      - Check 3 (CHANGELOG first heading): `grep -m1 '^## ' CHANGELOG.md | grep -qE "^## 2\.3\.0[[:space:]]" && echo "check3 PASS"` returns "check3 PASS". Implementer pastes the matched first heading. Verifies the FIRST `## ` heading in CHANGELOG matches `## 2.3.0` followed by any whitespace (em-dash `—` or space).
      - All three checks MUST pass; if any fails, the publish workflow would block the tag — this gate catches misalignments before commit. The patterns above are byte-identical with `.github/workflows/publish.yml:99/111/120` to guarantee parity.
    - **Phase 1 verification (REQ-01 / F-CORE-015):** `grep -r "function safeCreationTime" src/` returns EXACTLY 1 hit (the canonical at `src/pact/format.ts:138-140`). Implementer pastes the grep output — should be exactly one line: `src/pact/format.ts:138:function safeCreationTime(): string {`. If 0 hits OR 2+ hits, Phase 1 is incomplete or has regressed — implementer reports the failure with the full grep output. The grep must run against `src/` ONLY (NOT `dist/` or `tests/`).
    - **Phase 6 console-sweep verification (REQ-13 / F-CORE-022):** `grep -nE "console\.(warn|error)" src/` returns ZERO matches. Implementer pastes the grep output — should be empty (no lines). If ANY `console.warn` or `console.error` call is found in `src/`, Phase 6's sweep is incomplete — implementer reports the failure with the full grep output and the failing site(s). The grep must run against `src/` ONLY (NOT `dist/`, NOT `tests/`, NOT `.bee/`). NOTE: `console.log` calls are NOT in scope for this sweep — only `console.warn` and `console.error` (per audit-spec scope).
    - **package.json `./observability` exports-map verification (REQ-13 / cross-plan auto-fix iter 1 per CI-005):** explicit check that the new subpath landed in the exports map — without this, a misconfigured `package.json` could pass the 4-file dist assertion (T6.2/tsconfig produce dist files independently of T6.3) AND the runtime ESM import (which uses a relative `./dist/observability/index.js` path, not the package subpath), but consumers `import { setLogger } from "@stoachain/ouronet-core/observability"` would fail at module resolution post-publish. The check: `node -e "const e=require('./package.json').exports; if (!e['./observability'] || !e['./observability'].import || !e['./observability'].types) { console.error('FAIL: ./observability exports entry missing or malformed'); process.exit(1); } else { console.log('check PASS: ' + JSON.stringify(e['./observability'])); }"`. Implementer pastes the literal `check PASS: {...}` output. Expected JSON: `{"types":"./dist/observability/index.d.ts","import":"./dist/observability/index.js"}` (or with the keys in either order). If the check fails, Phase 6 T6.3 is incomplete — implementer reports the failure with the full output.
    - **Test-count + new-files sanity check:**
      - `git status --short` shows the expected NEW + MODIFIED files from all 7 phases:
        - **NEW files:** `src/observability/index.ts`, `src/observability/logger.ts` (Phase 6); plus any new test files added during Phase 2 / Phase 3 / Phase 6 (implementer enumerates from `git status --short tests/`).
        - **MODIFIED files:** `src/codex/codec.ts` (Phase 2), `src/signing/codexStrategy.ts` (Phase 2), `src/signing/types.ts` (Phase 2), 11× `src/interactions/*Functions.ts` (Phase 1 — activate, addLiquidity, coil, crossChain, dex, guard, kpay, ouro, pension, urStoa, wrap), `src/guard/smartAccountAuth.ts` (Phase 3), `src/guard/guardUtils.ts` (Phase 3), `src/guard/index.ts` (Phase 3 — `UnknownPredicateError` re-export), `src/reads/pactReader.ts` (Phase 6), `src/reads/rawCalibratedRead.ts` (Phase 6), any files touched by the console-sweep (Phase 6), `src/interactions/ouroFunctions.ts` (Phase 5 — also Phase 1 touched it; the sweep + DRY refactor land in the same file modification), `src/interactions/addLiquidityFunctions.ts` (Phase 5), `Z:/OuronetCore/README.md` (Phase 4 + Phase 7 / T7.3), `Z:/OuronetCore/.bee/CONTEXT.md` (Phase 4), `package.json` (Phase 6 + Phase 7 / T7.1), `CHANGELOG.md` (Phase 7 / T7.2).
      - Implementer pastes the `git status --short` output — sanity check that the expected files are all in the staging area / working tree.
      - Vitest summary line reports the expected per-phase NEW test counts: Phase 2 ≥3, Phase 3 ≥3, Phase 6 ≥3 (NFR-04). Implementer pastes the per-file count comparison from the vitest summary.
    - **Placeholder patching (post-`npm test` — F-004 / F-PAT-004 from v2.2.0 T2.13):** AFTER `npm test` produces the exact passing count `N`, the implementer (NOT the conductor) patches the placeholder tokens that T7.2 and T7.3 left in `CHANGELOG.md` and `README.md`. **Compute `M = N - 458` (delta from v2.2.0 baseline).** Three sites to patch:
      1. `README.md` test-count line at ~line 128 (T7.3's update 3 first site): `**≥{N} tests**` → `**N tests**` AND `+{M} new tests across` → `+M new tests across` (using the actual integers; the trailing `tests across the v2.3.0 audit-closure surfaces — codex-codec shape validation, foreign-key resolver pre-flight, guard classification + keyset-ref normalization + 4-state analysis + UnknownPredicateError, observability seam contract` clause is permanent prose from T7.3 — DO NOT remove it).
      2. `README.md` vitest-recipe line at ~line 313 (T7.3's update 3 second site): `vitest run — ≥{N} tests across...` → `vitest run — N tests across...` (the comma-separated test-file list including the new `, observability` suffix is unchanged from T7.3).
      3. `CHANGELOG.md` Stats block (T7.2's Stats section): `Test count: **≥{N}** passing` → `Test count: **N** passing` (bold wraps the NUMBER ONLY, not the word "passing" — matches v2.2.0 convention) AND `+{M} new in v2.3.0` → `+M new in v2.3.0`. NO build-internal text leaks into the published CHANGELOG (F-303 lock from v2.2.0).
      Implementer runs `grep -nE "≥\{N\}|\+\{M\}|\{N\}|\{M\}" README.md CHANGELOG.md` AFTER the patch. Expected output: EMPTY (no remaining placeholders). If any match remains, the patch is incomplete and must be re-run before the gate passes.
      Then implementer re-runs the workflow-gate Check 1 and Check 2 from above (the patched README must STILL pass both grep checks — the patches only replace the test-count placeholders, NOT the Status-block version anchor or the version-history paragraph anchor) to confirm the README is still in publish-ready shape.
    - **No-regression sanity check:** the 458 v2.2.0 baseline tests still pass. Implementer compares the failure list (if any non-zero exit) against the documented Windows-locale exception only. ANY other failure is a regression that must be reported with full output, NOT swept into "passes within tolerance".
    - **NFR-02 type-regression lock:** `tests/types.test.ts` v1.7.0 type-regression lock continues to pass. Implementer confirms by reading the per-file vitest summary line for `tests/types.test.ts` — its passing count must be unchanged from the v2.2.0 baseline. If the count differs, the additive-only contract has been violated by one of Phases 1-6 — implementer reports the failure.
    - **NFR-05 no-open-handles:** vitest summary contains NO "Open handles" warnings. Implementer pastes the summary's tail (last 10 lines) as evidence. If any open-handle warning is reported, the failure must be reported (do NOT silently sweep — open handles can mask test isolation bugs).
    - **Atomic-ship contract spot-check (NFR-06):** `git status` shows the changes from all 7 phases are staged in a single commit-ready state. The conductor (NOT this implementer) creates the actual commit and tag via `/bee:commit` AFTER this gate passes. The implementer's job is to confirm the working tree is in the right shape FOR the conductor's commit — paste `git diff --stat` output enumerating the changed files (expected: ~25-30 files modified or added across the 7 phases).
    - If any quality gate fails, implementer reports the failure with paste of the failing output rather than claiming success. The conductor decides whether to spawn a fixer agent or escalate.
  - context: requirements.md REQ-14 / REQ-15 / REQ-16 / REQ-17; CLAUDE.md "Common commands" block; v2.2.0 test-count baseline of 458 (`README.md:128` `**458 tests** pass on every commit`); the documented Windows-locale exception locked from v2.1.2 / v2.2.0 per ROADMAP.md success criterion 5; spec.md "Release artifacts" fourth + fifth bullets (workflow-gate three-check + verification gate); the publish workflow at `.github/workflows/publish.yml` (the version-parity gate at lines 99/111/120 — implementer reads this file to confirm the exact grep patterns); v2.2.0 T2.13 acceptance from `.bee/archive/2026-05-02-crypto-pact-test-hardening/phases/02-test-coverage-for-critical-untested-surfaces-and-release-artifacts/TASKS.md:347-385` (the precedent for the verification-gate evidence-paste discipline + the placeholder-patching procedure); LOCKED dependency on Phases 1-6 having all landed in the working tree before T7.4 runs (per the cross-phase dependency block at the top of this file).
  - research:
    - Pattern: [CITED] No prior verification-gate test file — this task runs commands and pastes output. Closest precedent is v2.2.0 T2.13 (`TASKS.md:347-385`), which establishes the evidence-paste discipline (R8) precedent and the placeholder-patching procedure. Mirror its acceptance structure exactly: per-command output paste, per-grep output paste, per-patch-step grep verification, atomic-ship spot-check via `git diff --stat`.
    - Reuse: [CITED] Workflow-gate grep patterns from `.github/workflows/publish.yml:99/111/120` — the three patterns the local verification mirrors. Bash-equivalent on Windows: same `grep -E` syntax (Bash is the shell per the env block; project is run from a Bash subshell — verified per the env declaration).
    - Reuse: [CITED] Quality-gate commands per CLAUDE.md "Common commands" — `npm run typecheck`, `npm test`, `npm run build`, `npm run clean`. The clean-before-build discipline catches stale `dist/` issues (auto-fix iter 1 per F-001 from v2.2.0 — same rationale applies here for the new `./observability` subpath).
    - Reuse: [CITED] Windows-locale `tests/gas.test.ts > formatMaxFee` exception locked at v2.1.2 / v2.2.0 baseline per ROADMAP.md success criterion 5 — sole allowed non-zero exit on Windows non-en-US runners.
    - Reuse: [CITED] Phase 1 verification: `grep -r "function safeCreationTime" src/` from requirements.md REQ-01 (`grep -r "function safeCreationTime" src/` returns exactly one hit post-phase). Phase 6 verification: `grep -nE "console\.(warn|error)" src/` from requirements.md REQ-13 (returns ZERO matches post-phase).
    - Reuse: [CITED] Placeholder-patching grep pattern: `grep -nE "≥\{N\}|\+\{M\}|\{N\}|\{M\}" README.md CHANGELOG.md` from v2.2.0 T2.13 acceptance line 369-373 — the 4 placeholder shapes (`≥{N}`, `+{M}`, `{N}`, `{M}`); auto-fix iter 2 per F-006 from v2.2.0 REMOVED the `≥[0-9]+` alternative (it was over-eager and matched intentional `≥29`/`≥34` literals). The 4-shape pattern is the exact one to use here.
    - Types: N/A — command-runner task.
    - Approach: [LOCKED iter 1 per cross-plan CI-004] Test count floor ≥467 (strict NFR-04: ≥3 per substantive phase × 3 substantive phases = ≥9 new tests + 458 v2.2.0 baseline = ≥467). Per-phase commitments are higher: Phase 2 +8, Phase 3 +10, Phase 6 +4 = realistic floor ≥480. Implementer reports EXACT count from `npm test` output. Wall-time check non-strict: <30s hard fail, >25s informational note. The conductor (NOT implementer) creates the actual commit and tag — implementer confirms working tree shape only and pastes `git diff --stat` as evidence.

  - notes:

---

## Wave dependency chain

```
Wave 1 (3 tasks, fully parallel — file-disjoint)
├── T7.1 → package.json     (MODIFY, exclusive)  [single-line version-string edit]
├── T7.2 → CHANGELOG.md     (MODIFY, exclusive)  [prepend ## 2.3.0 entry with placeholders]
└── T7.3 → README.md        (MODIFY, exclusive)  [Status + history + test-count placeholders + observability row + What's new]
        │
        ▼
Wave 2 (1 task — sequential gate by design)
└── T7.4 → verification gate (no source-file modifications; patches CHANGELOG.md + README.md placeholders post-test)
           needs: T7.1, T7.2, T7.3
           runs: typecheck + test + clean + build + 4-file dist assertion + runtime export check + 3 workflow-gate greps
                 + Phase 1 grep + Phase 6 grep + placeholder patching + atomic-ship spot-check
           cross-phase precondition: Phases 1-6 already landed in working tree (per atomic-ship contract NFR-06;
                                     conductor's wave logic guarantees this before Phase 7 spawns)
```

**File ownership conflict scan:** Each of the 3 Wave 1 tasks owns a distinct file (T7.1 → `package.json`, T7.2 → `CHANGELOG.md`, T7.3 → `README.md`). Zero pairwise intersections — no two Wave 1 tasks ever touch the same file. T7.4 modifies `CHANGELOG.md` and `README.md` in the placeholder-patching step but runs in a strictly later wave (Wave 2), so no conflict with Wave 1.

**T7.1 / T7.2 / T7.3 logical contract (file-disjoint, contract-coupled):** the three release artifacts (package.json bump, CHANGELOG prepend, README update) share a logical version-parity contract — all three must agree on `2.3.0`. They touch disjoint files so they parallelize at the file level. T7.4 verifies the contract holds via the three workflow-gate grep checks AFTER all three land. This is the established design pattern for atomic-release tasks (verified at v2.2.0 T2.10 / T2.11 / T2.12 / T2.13).

**Cross-phase dependency note:** Phase 7 has a HARD dependency on Phases 1-6 having all landed in the working tree before T7.4 runs. The atomic-ship contract (NFR-06) requires all 7 phases to land in a SINGLE commit + a SINGLE annotated `v2.3.0` tag, so the conductor's wave logic ensures Phases 1-6 produce their on-disk artifacts before Phase 7 spawns. T7.4's Phase 1 grep (`grep -r "function safeCreationTime" src/` returns 1) and Phase 6 grep (`grep -nE "console\.(warn|error)" src/` returns 0) explicitly assert that the prior phases have completed cleanly. T7.4's 4-file dist assertion (`dist/observability/{index,logger}.{js,d.ts}` all 4 present) explicitly assert Phase 6's source files are on disk and compile cleanly. T7.4's runtime export verification asserts the seam loads via dynamic ESM import. T7.4's `≥467` test count floor (LOCKED iter 1 per cross-plan CI-004) asserts the cumulative ≥9 NEW test cases from Phases 2 + 3 + 6 (NFR-04: ≥3 each × 3 substantive phases) have all landed.

## Fragmentation Note

**Wave structure: HEALTHY.**

- Total tasks: 4
- Total waves: 2
- Average tasks per wave: 4 / 2 = **2.0 tasks/wave**
- Target: ≥2.5 tasks/wave (consolidation goal)
- Status: 2.0 / 2.5 = 0.8× target — BELOW the consolidation target.

**Below-target rationale (no fragmentation candidates exist):** Wave 2 contains a single task by genuine sequential design — T7.4's verification gate asserts on the integrated state of all 3 Wave 1 deliverables (typecheck the full source tree post-Phase-1-6, run the FULL vitest suite including new + extended tests, build the dist barrel, run grep checks across CHANGELOG.md + README.md + package.json after T7.1 / T7.2 / T7.3 land, plus cross-phase grep checks asserting Phase 1's safeCreationTime DRY and Phase 6's console-sweep landed cleanly). This is a genuine sequential dependency — the gate's purpose is to catch integration failures across all Wave 1 tasks AND across all 6 prior phases before the conductor commits. Pulling T7.4 into Wave 1 would defeat the gate's contract (it would run before T7.1 / T7.2 / T7.3 produced their files).

**Wave 1 has 3 tasks — within the 3-5-tasks-per-wave consolidation target.** The release-artifacts pattern is intrinsically 3 tasks (one per file: package.json, CHANGELOG.md, README.md) plus a verification gate; this is the established v2.2.0 pattern (which ran 12 tasks in Wave 1 only because it bundled new-test-file authoring with the 3 release-artifact tasks). For Phase 7 of this spec, the 3 release-artifact tasks are the only Wave 1 candidates — there is no test-authoring work in this phase (NFR-04 floors are owned by Phases 2 / 3 / 6).

**Average-below-target is acceptable here** because (a) the 1-task Wave 2 is justified by genuine integration-state-assertion semantics and (b) Wave 1 cannot be expanded — there is no additional file-disjoint work that fits Phase 7's release-artifacts-only scope. This is the same pattern as v2.2.0's Phase 2 verification gate (which also ran as a 1-task Wave 2 by design — see v2.2.0 TASKS.md "Fragmentation Note" lines 419-431).

---

## Pass 1 + 2 completion notes

- **Task count:** 4 tasks (T7.1 through T7.4).
- **Requirements mapped:** REQ-14 → T7.1; REQ-15 → T7.2; REQ-16 → T7.3; REQ-17 → T7.4. All 4 phase-7 requirements have at least one task. Cross-cutting NFR-02 / NFR-03 / NFR-05 / NFR-06 are asserted in T7.4's verification gate.
- **Wave count:** 2 waves (Wave 1 parallel × 3 tasks; Wave 2 sequential gate × 1 task).
- **File-ownership conflicts detected:** 0 (T7.1 / T7.2 / T7.3 are file-disjoint by design; T7.4 modifies files in a strictly later wave).
- **Research coverage:** all 4 tasks have CITED `research:` blocks sourced from requirements.md REQ-14..REQ-17, the v2.2.0 archive precedent (T2.10/T2.11/T2.12/T2.13), the publish workflow at `.github/workflows/publish.yml:91-128`, and the existing CHANGELOG / README / package.json files.
- **Cross-phase dependency:** Phase 7 has a HARD dependency on Phases 1-6 having all landed in the working tree (per NFR-06 atomic-ship contract). T7.4's Phase 1 grep + Phase 6 grep + 4-file dist assertion + runtime export check explicitly assert this.
- **Fragmentation status:** average 2.0 tasks/wave is below the 2.5 target but justified by genuine sequential semantics on Wave 2 + intrinsic 3-file scope on Wave 1 (matches v2.2.0 pattern). No 1-2 task waves are merge candidates.
