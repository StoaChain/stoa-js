# Project Index

> Auto-generated by `/bee:init`. Re-run init to refresh.

## Structure
```
.
├── .github/workflows/      (ci.yml, publish.yml)
├── src/
│   ├── codex/              (codec, seedTypeMigration, types)
│   ├── constants/          (kadena, tokenIds)
│   ├── crypto/             (v1, v2 — AES-GCM-256 + smartDecrypt)
│   ├── dalos/              (account, index — DALOS registry re-export)
│   ├── errors/             (transactionErrors)
│   ├── gas/                (gasUtils)
│   ├── guard/              (guardUtils, smartAccountAuth)
│   ├── interactions/       (13 *Functions.ts files — Pact builders/execs)
│   ├── network/            (nodeFailover)
│   ├── pact/               (cfmBuilders, format)
│   ├── reads/              (pactReader, rawCalibratedRead)
│   ├── signing/            (codexStrategy, primitives, types, universalSign)
│   └── wallet/             (KadenaWallet, KadenaWalletBuilder, storageAdapter, types)
└── tests/                  (12 *.test.ts files)
```

## Entry Points
- `src/index.ts` — root barrel (intentionally near-empty; consumers use subpath exports)

## Routes
(none — published library, no HTTP surface)

## Config Files
- `package.json` — npm package + subpath exports map
- `tsconfig.json` — typecheck config (`noEmit: true`, strict)
- `tsconfig.build.json` — build config (extends tsconfig, emits to `dist/`, excludes tests)
- `vitest.config.ts` — vitest setup (node env, picks up `tests/**` and `src/**/*.test.ts`)
- `.github/workflows/ci.yml` — typecheck + test + build on PR/push
- `.github/workflows/publish.yml` — `npm publish` on `v*` tags

## Models / Schema
(none — library code only, no ORM)

## Tests
- Top-level `tests/` directory (not co-located)
- 12 test files: cfm-builders, codex-codec, dalos-integration, encryption, encryption-upgrade, gas, guard, network, pact-format, signing, smart-account-auth, strategy
- Vitest also globs `src/**/*.test.ts` (none currently)

## Dependencies
### Runtime
- `@stoachain/dalos-crypto` (DALOS cryptography registry)

### Peer (consumers must install)
- `@kadena/client`, `@kadena/cryptography-utils`, `@kadena/hd-wallet`, `@kadena/types`
- `@noble/curves`, `@scure/bip39`

### Dev
- `typescript`, `vitest`, `rimraf`, `@types/node`
- (peer deps duplicated in dev for local typecheck/test)

## Stats
- Total files (excl. node_modules/.git/dist): 76
- src: 52 files (TypeScript sources)
- tests: 12 files (vitest specs)
- .github: 2 workflow files

## Notes
- Pure TypeScript library — no UI, no server, no runtime entry.
- Subpath-export model: 14 subpaths declared in `package.json#exports` (`./guard`, `./signing`, `./interactions/*`, `./dalos`, etc.).
- Published as `@stoachain/ouronet-core` on public npmjs (scope `@stoachain`).
- Consumed by OuronetUI (browser SPA) and AncientHolder HUB (Node.js server).
