{
  "name": "stoa-js",
  "version": "0.0.0",
  "description": "StoaChain TypeScript stack — workspace root for @stoachain/stoa-core (chain-generic foundation) and @stoachain/ouronet-core (Ouronet protocol business logic).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@stoachain/stoa-core && npm run build --workspace=@stoachain/ouronet-core",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces",
    "test:watch": "npm run test:watch --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/StoaChain/stoa-js.git"
  },
  "license": "UNLICENSED"
}
